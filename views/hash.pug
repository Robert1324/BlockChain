extends layout

block content
  .container

    h1 SHA256 #{__('Hash')}
    .well#well1
      form.form-horizontal

        .form-group
          label.col-sm-2.control-label(for='data') #{__('Image')}:
          .col-sm-10
            //textarea#data.form-control(rows='10', onkeyup='$("#hash").val(sha256())')
            input#data.upload-file(type="file", name="upload")
            input#data.button(type="button", value="Hash", onclick='$("#hash").val(sha256())')

        .form-group
          label.col-sm-2.control-label(for='hash') #{__('Hash')}:
          .col-sm-10
            input#hash.form-control(type='text', disabled)

  script.
    function getText(block, chain) {
      return $('#data').val();
    }

    $("#hash").val(sha256());
